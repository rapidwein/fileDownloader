from setuptools import setup

setup(name='fileDownloader',
      version='0.1',
      description='ftp client and server',
      url='https://github.com/rapidwein/fileDownloader',
      author='varun',
      install_requires=[
	'pyftpdlib',
	'urlgrabber',
	])
